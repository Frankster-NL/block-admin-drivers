# Ninja Driver Install

Install drivers on a ninja block.

## Boot Instructions:
```shell
git clone git@github.com:timoxley/ninja-driver-install.git
cd ninja-driver-install
npm install
make -B
DEBUG=* ./bin/server
open http://localhost:9099
```
